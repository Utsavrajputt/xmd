<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?android:attr/colorBackground">

    <!-- Search bar - hidden by default, revealed by the toolbar search
         icon (MainActivity.onOptionsItemSelected -> DownloadsFragment).
         Same visual pattern as HistoryFragment's search card. -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/queueSearchCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:visibility="gone"
        app:cardBackgroundColor="?attr/colorSurfaceContainerLow"
        app:shapeAppearance="@style/ShapeAppearance.Xmd.Large"
        app:cardElevation="0dp"
        app:strokeWidth="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="12dp"
            android:paddingEnd="12dp">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_search"
                android:tint="?attr/colorOnSurfaceVariant"
                android:contentDescription="@null" />

            <EditText
                android:id="@+id/queueSearchInput"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:background="@null"
                android:hint="@string/queue_search_hint"
                android:inputType="text"
                android:imeOptions="actionSearch"
                android:maxLines="1"
                android:singleLine="true"
                android:textColor="?attr/colorOnSurface"
                android:textColorHint="?attr/colorOnSurfaceVariant"
                android:textSize="14sp" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Summary bar: horizontally-scrolling row of stat pills, e.g.
         "2 downloading"  "3 done"  "1 failed" -->
    <HorizontalScrollView
        android:id="@+id/queueSummaryBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurfaceContainerLow"
        android:scrollbars="none"
        android:visibility="gone">

        <com.google.android.material.chip.ChipGroup
            android:id="@+id/queueSummaryChips"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingHorizontal="12dp"
            android:paddingVertical="8dp"
            app:singleLine="true"
            app:chipSpacingHorizontal="8dp" />
    </HorizontalScrollView>

    <!-- Main content area: recycler OR empty label -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/queueRecycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:paddingHorizontal="12dp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp"
            android:clipToPadding="false" />

        <LinearLayout
            android:id="@+id/emptyContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:visibility="gone">

            <FrameLayout
                android:id="@+id/emptyIconFrame"
                android:layout_width="88dp"
                android:layout_height="88dp"
                android:background="@drawable/bg_icon_button_tonal"
                android:layout_marginBottom="18dp">

                <ImageView
                    android:id="@+id/emptyIcon"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_downloads"
                    android:tint="?attr/colorOnSecondaryContainer"
                    android:contentDescription="@null" />
            </FrameLayout>

            <TextView
                android:id="@+id/emptyLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="No downloads yet"
                android:textColor="?attr/colorOnSurface"
                android:textStyle="bold"
                android:textSize="16sp" />

            <TextView
                android:id="@+id/emptySubLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:text="Add links in the Home tab"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="12sp" />
        </LinearLayout>
    </FrameLayout>

    <!-- Cancel All / Retry All (context-switches) + Clear All -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_margin="12dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/cancelButton"
            style="@style/Widget.Xmd.Button.Outlined"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_marginEnd="4dp"
            android:text="@string/action_cancel_all"
            app:strokeColor="?attr/colorError"
            android:textColor="?attr/colorError"
            android:visibility="gone" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/clearAllButton"
            style="@style/Widget.Xmd.Button.Tonal"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:text="@string/action_clear_all"
            android:visibility="gone" />
    </LinearLayout>

</LinearLayout>
