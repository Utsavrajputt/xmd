<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="72dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:paddingHorizontal="4dp"
    android:paddingVertical="4dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?attr/selectableItemBackgroundBorderless">

    <!-- Ring: shows the selection border (transparent when not selected) -->
    <FrameLayout
        android:id="@+id/swatchRing"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:padding="3dp">

        <!-- Box: filled with the theme's background color, rounded corners -->
        <FrameLayout
            android:id="@+id/swatchBox"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:orientation="horizontal">

                <View
                    android:id="@+id/dotPrimary"
                    android:layout_width="12dp"
                    android:layout_height="12dp"
                    android:layout_margin="2dp" />

                <View
                    android:id="@+id/dotSecondary"
                    android:layout_width="12dp"
                    android:layout_height="12dp"
                    android:layout_margin="2dp" />

                <View
                    android:id="@+id/dotTertiary"
                    android:layout_width="12dp"
                    android:layout_height="12dp"
                    android:layout_margin="2dp" />
            </LinearLayout>

            <ImageView
                android:id="@+id/checkIcon"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_gravity="bottom|end"
                android:layout_margin="3dp"
                android:src="@drawable/ic_check"
                android:visibility="gone" />
        </FrameLayout>
    </FrameLayout>

    <TextView
        android:id="@+id/themeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="11sp"
        android:maxLines="1"
        android:ellipsize="end" />

</LinearLayout>
